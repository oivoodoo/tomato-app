describe('Pomodoros Router', function() {
  beforeEach(function() {
    this.controller = App.Routers.Pomodoros();
  });

  it("should use list of pomodoros previously saved to the local storage", function() {
  });
});
